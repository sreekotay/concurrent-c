#include <ccc/std/prelude.cch>

int main(void) {
    @errhandler(CCError e) { cc_error_log(e); return 1; }
    CCArena arena = cc_arena_heap(kilobytes(64)) @destroy;
    CCTurnstile ts@(2, 1, arena) !> @destroy;
    int s = 0;
    int i;
    @parallel wait (ts) cache (s) @for (i in 0..4) {
        @stage (ts, 0, i) {
            s++;
        }
    } !>.wait()!>;
    return s;
}
