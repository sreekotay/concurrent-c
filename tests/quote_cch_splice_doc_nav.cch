#ifndef TESTS_QUOTE_CCH_SPLICE_DOC_NAV_CCH
#define TESTS_QUOTE_CCH_SPLICE_DOC_NAV_CCH

/* Interface leaf — extracts on its own. SpliceDoc* must not pull the
 * program-member parent into the `.h`; the program defines it. */
static int splice_doc_touch(const SpliceDoc *d);

#endif
