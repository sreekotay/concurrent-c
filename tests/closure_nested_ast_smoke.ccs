// Smoke: nested closures should be *recorded* in stub AST (Phase 2).

int g = 0;
int x = 1;

int main(void) {
    @nursery {
        spawn (() => {
            @nursery {
                spawn (() => { g = x; });
            }
        });
    }
    return 0;
}

