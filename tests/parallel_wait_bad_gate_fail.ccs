#include <ccc/std/prelude.cch>

int main(void) {
    @errhandler(CCError e) { cc_error_log(e); return 1; }
    int gate = 3;
    int i;
    int xs[4];
    @parallel wait (gate) for (i in 0..4) {
        xs[i] = i;
    }
    return xs[0];
}
