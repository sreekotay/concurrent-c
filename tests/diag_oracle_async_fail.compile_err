# Oracle: an @async state machine (~20x line expansion) above the error must
# not shift reported lines (masked #line resync after the expansion; the
# await_normalize inserts are line-neutral so the resume count is exact).
# Pin file:line + symbol (column/`error:` optional — see plain_fail).
diag_oracle_async_fail.ccs:16:
cc_diag_oracle_undeclared
