// NEGATIVE: including a grammar that was never declared in this file.
#include <ccc/std/prelude.cch>

@grammar(rules) Bad {~~~~
    top: digit
    include Missing
~~~~}

int main(void) { (void)Bad_match("1", 1); return 0; }
