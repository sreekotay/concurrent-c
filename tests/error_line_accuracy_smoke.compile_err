# Verify error line numbers remain accurate after compiler transformations
# (@destroy -> @defer, UFCS rewriting, @slice expansion).
# The undeclared function is on source line 14 and must be reported there:
# the defer return-variable prologue is line-neutral since the diagnostics
# wave (it rides the function's `{` line).  See the diag_oracle_* corpus.
error_line_accuracy_smoke.ccs:14
this_function_does_not_exist
