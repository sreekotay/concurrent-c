# Oracle: exact user-file coordinates for a host-compiler diagnostic with
# no CC features above the error line (baseline #line sourcemap fidelity).
# Pin file:line:col + symbol; clang wording varies
# ("implicit declaration" vs "call to undeclared function").
diag_oracle_plain_fail.ccs:8:10: error:
cc_diag_oracle_undeclared
