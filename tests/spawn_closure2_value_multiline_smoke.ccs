#include "cc_closure.cch"
#include "cc_nursery.cch"

int main(void) {
    int g = 0;
    int* gp = &g;
    int a = 6;
    int b = 7;

    CCClosure2 c = (int x, int y) => { *gp = x + y; };

    @nursery {
        spawn (c,
              a,
              b);
    }
    return (g == 13) ? 0 : 1;
}

