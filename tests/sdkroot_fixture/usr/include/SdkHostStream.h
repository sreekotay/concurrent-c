#ifndef TESTS_SDKROOT_FIXTURE_SDK_HOST_STREAM_H
#define TESTS_SDKROOT_FIXTURE_SDK_HOST_STREAM_H

/* Same shape as SDK zlib.h: `typedef struct tag { … } Name`. */
typedef struct sdk_host_stream_s {
    int n;
} SdkHostStream;

#endif
