#!/usr/bin/env -S ./cc/bin/ccc
/*
 * Compile-fail: CCDoc @task on an incompatible parameter list.
 */

/**
 * @task Not a valid task signature
 */
static int bad(int x) {
    return x;
}

/* Unreachable: compile must fail on @task shape above. */
return 0;
