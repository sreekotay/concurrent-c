/* p and ^p in the same group. */
#include <ccc/std/prelude.cch>

typedef struct Bag {
    int secret;
    int len;
} Bag;

@typeview on Bag {
    r: secret, ^secret;
};

int main(void) {
    return 0;
}
