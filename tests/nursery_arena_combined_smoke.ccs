/* Test: owned nursery and owned arena in the same function.
   
   This tests the EditBuffer fix - previously nursery and arena rewrites used the same AST
   but nursery would modify the source first, leaving arena lowering with stale offsets.
   Now both collect edits into a shared EditBuffer and apply them together.
*/

#include <ccc/std/prelude.cch>
#include <stdio.h>
#include <stdlib.h>

void test_nursery_then_arena(void) {
    @errhandler(CCError e) cc_error_exit(e);
    printf("test_nursery_then_arena:\n");
    
    {
        CCNursery __cc_nursery60 = @create() !> @destroy;

        __cc_nursery60.spawn(() => {
            printf("  spawned task\n");
        }) !>;
    
    }
    
    {
        CCArena a = @create(kilobytes(4)) @destroy;
        if (!cc_arena_is_live(a)) abort();
        int* p = cc_arena_alloc_T(int, a);
        *p = 42;
        printf("  arena allocated: %d\n", *p);
    
    }
    
    printf("  done\n");
}

void test_arena_then_nursery(void) {
    @errhandler(CCError e) cc_error_exit(e);
    printf("test_arena_then_nursery:\n");
    
    {
        CCArena a = @create(kilobytes(4)) @destroy;
        if (!cc_arena_is_live(a)) abort();
        int* p = cc_arena_alloc_T(int, a);
        *p = 123;
        printf("  arena allocated: %d\n", *p);
    
    }
    
    {
        CCNursery __cc_nursery61 = @create() !> @destroy;

        __cc_nursery61.spawn(() => {
            printf("  spawned task\n");
        }) !>;
    
    }
    
    printf("  done\n");
}

void test_interleaved(void) {
    @errhandler(CCError e) cc_error_exit(e);
    printf("test_interleaved:\n");
    
    {
        CCArena a1 = @create(kilobytes(4)) @destroy;
        if (!cc_arena_is_live(a1)) abort();
        printf("  arena a1 active\n");
        
        {
            CCNursery __cc_nursery62 = @create() !> @destroy;

            __cc_nursery62.spawn(() => {
                printf("  spawned inside arena\n");
            }) !>;
        
        }
        
        printf("  nursery inside arena done\n");
    
    }
    
    {
        CCNursery __cc_nursery63 = @create() !> @destroy;

        {
            CCArena a2 = @create(kilobytes(4)) @destroy;
            if (!cc_arena_is_live(a2)) abort();
            printf("  arena inside nursery\n");
        
        }
        
        __cc_nursery63.spawn(() => {
            printf("  spawned after arena\n");
        }) !>;
    
    }
    
    printf("  done\n");
}

int main(void) {
    test_nursery_then_arena();
    test_arena_then_nursery();
    test_interleaved();
    printf("All combined tests passed!\n");
    return 0;
}
