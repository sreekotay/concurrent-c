#!/usr/bin/env -S ./cc/bin/ccc
/*
 * Compile-fail: @task must not accept int*,char* (false argc/argv shape).
 */

/**
 * @task Not argc/argv
 */
static int bad(int *count, char *name) {
    (void)count;
    (void)name;
    return 0;
}

/* Unreachable: compile must fail on @task shape above. */
return 0;
