/* Nested impl-grade `.cch` without a sibling `.ccs` does not splice
 * through an extracted umbrella. */
#include "quote_cch_nested_impl_via_umbrella.cch"

int main(void) {
    return nested_impl_via_umbrella_decl();
}
