/* Explicit Gap: #include <stdlib.h> declares malloc. */
#include <ccc/std/prelude.cch>
#include <ccc/stdio.cch>
#include <stdlib.h>

int main(void) {
    void *p = malloc(16);
    if (!p) return 1;
    free(p);
    return 0;
}
