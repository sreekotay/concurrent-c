// Slice 7: by default, a bare call to a result-returning function at
// statement position is a compile error.  No env: this pins the default.
#include <stdio.h>
int !> (CCError) f(void) { return cc_err(CC_ERR_NOT_FOUND, "x"); }
int main(void) {
    f();
    printf("done\n");
    return 0;
}
