// Slice 7: with CC_STRICT_RESULT_UNWRAP=1, a bare call to a
// result-returning function at statement position is a compile error.
#include <stdio.h>
int !> (CCError) f(void) { return cc_err(CC_ERR_NOT_FOUND, "x"); }
int main(void) {
    f();
    printf("done\n");
    return 0;
}
