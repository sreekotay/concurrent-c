#!ccc ccs
#include "ufcs_header_proto_family.cch"
#include <stdio.h>

int main(void) {
    UfcsHdrBox b = { .n = 3 };
    size_t n = b.len();
    char[:] s = b.span();
    if (n != 3) return 1;
    if (s.len != 2) return 2;
    printf("ok\n");
    return 0;
}
