/* `::[...]` binds only members that declare a type formal. */
#include <ccc/std/prelude.cch>
int main(void) {
    CCArena arena = cc_arena_heap(kilobytes(64));
    Vec::[int] v = vec_new::[int](&arena);
    v.push::[int](1);
    return 0;
}
