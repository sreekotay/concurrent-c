/* Copy walk is not a Result. `!>` is the unwrap signal — not decoration. */
#include <ccc/std/prelude.cch>

int main(void) {
    int[:] xs = { 1, 2 };
    @for (v in xs) {
        (void)v;
    } !>;
    return 0;
}
