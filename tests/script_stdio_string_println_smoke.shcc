#!/usr/bin/env -S ./cc/bin/ccc
/*
 * Smoke: flipped println/eprintln on CCString and slice (UFCS on the data).
 */

CCArena arena = @create(kilobytes(64)) @destroy;
CCString s = @string(`stdio-string-ok`, arena);
s.println();
s.eprintln();
@slice("slice-ok").println();
return 0;
