// Compile-fail: provenance-driven move-only slice cannot be copied.
#include "cc_runtime.cch"

int main(void) {
    int* p = (int*)malloc(sizeof(int) * 2);
    p[0] = 1; p[1] = 2;
    /* move-only by value provenance: id unique bit is set */
    CCSlice s = cc_slice_from_parts(p, 2, cc_slice_make_id(1, true, false, false), 2);
    CCSlice t = s;             // ERROR: copy
    (void)t;
    return 0;
}

