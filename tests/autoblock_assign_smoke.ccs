#include "cc_runtime.cch"
#include "cc_closure.cch"
#include <stdint.h>

static int add1(int x) { return x + 1; }

@async int f(int x) {
    int y = 0;
    y = add1(x);
    return y;
}

int main(void) {
    return f(1) == 2 ? 0 : 1;
}

