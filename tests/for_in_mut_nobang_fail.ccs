/* Mut walk is void !>(CCError): the write-time .len re-check is the error. */
#include <ccc/std/prelude.cch>

int main(void) {
    char[:] s = {'a'};
    @for (&ch in s)
        ch = 'x';
    return 0;
}
