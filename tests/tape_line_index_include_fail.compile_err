# Included tape's line index: `#line 77 "virt_include.cch"` inside the face
# makes the next line logical 77 of that file, and the diagnostic names it
# so — not the .cch physical line, not a line in the including .ccs.
virt_include.cch:77
expected
