/* Field `@as` is removed; use `@typeview on T { as: field; }`. */
#include <ccc/std/prelude.cch>
#include <stdio.h>

typedef struct {
    CCFile file @as;
    int x;
} AnonOuter;

int main(void) {
    AnonOuter o = {0};
    (void)o;
    printf("unreachable\n");
    return 0;
}
