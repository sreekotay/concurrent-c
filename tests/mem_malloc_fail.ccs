/* Default CCS includes do not declare malloc. Include <stdlib.h> to use it. */
#include <ccc/std/prelude.cch>
#include <ccc/script/stdio.cch>

int main(void) {
    void *p = malloc(16);
    return p ? 0 : 1;
}
