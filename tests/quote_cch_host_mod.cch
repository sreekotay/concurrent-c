#ifndef TESTS_QUOTE_CCH_HOST_MOD_CCH
#define TESTS_QUOTE_CCH_HOST_MOD_CCH

/* Module face: exported `host_mod_tag` is one definition in the module
 * object. Includes a differently-stemmed impl guest and a peer, modules
 * of their own. Every includer extracts decls. */
#include "quote_cch_guest_leaf.cch"
#include "quote_cch_peer_mod.cch"

int host_mod_tag(void) { return 1; }

#endif
