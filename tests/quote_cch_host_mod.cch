#ifndef TESTS_QUOTE_CCH_HOST_MOD_CCH
#define TESTS_QUOTE_CCH_HOST_MOD_CCH

/* Owned face: non-static `host_mod_tag` is one definition in this TU.
 * Includes a differently-stemmed impl guest and a separately-owned peer.
 * Other TUs extract decls; the guest is still owned. */
#include "quote_cch_guest_leaf.cch"
#include "quote_cch_peer_mod.cch"

int host_mod_tag(void) { return 1; }

#endif
