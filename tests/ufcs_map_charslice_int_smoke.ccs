#include <ccc/std/prelude.cch>
#include <stdio.h>

int main(void) {
    CCArena arena = cc_arena_heap(kilobytes(4));
    if (!arena.base) return 1;

    Map<char[:], int> m = map_new<char[:], int>(&arena);

    char[:] k1 = @slice("alpha");
    char[:] k2 = @slice("beta");
    char[:] k3 = @slice("gamma");

    m.insert(k1, 11);
    m.insert(k2, 22);
    m.insert(k3, 33);

    int* got = m.get_ptr(k2);
    if (!got || *got != 22) {
        printf("FAIL: get_ptr(beta) expected 22\n");
        return 2;
    }

    int removed = m.remove(k2);
    if (!removed) {
        printf("FAIL: remove(beta) should return true\n");
        return 3;
    }

    int* after = m.get_ptr(k2);
    if (after) {
        printf("FAIL: get_ptr(beta) should be NULL after remove\n");
        return 4;
    }

    printf("ufcs_map_charslice_int_smoke: PASS\n");
    cc_arena_free(&arena);
    return 0;
}
