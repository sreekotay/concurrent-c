/* Fail oracle: `@variant(packed)` (spec §12 niche packing) is a later
 * phase; the surface refuses loudly rather than silently falling back to
 * the unpacked layout. */
#include <ccc/std/prelude.cch>
#include <stdint.h>

@variant(packed) V {
    a: const char*;
    b: int64_t;
};

int main(void) { return 0; }
