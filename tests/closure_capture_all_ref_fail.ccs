/* Test: capture-all by reference [&] is banned */
#include <ccc/cc_nursery.cch>
#include <stdlib.h>

int main(void) {
    @errhandler(CCError e) { (void)e; abort(); }
    int x = 0;
    
    {
        CCNursery __cc_nursery31 = @create() !> @destroy;

        /* ERROR: capture-all [&] is not allowed */
        __cc_nursery31.spawn(() => [&] {
            x++;
        });
    
    }
    
    return 0;
}
