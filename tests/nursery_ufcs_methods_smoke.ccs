#include <ccc/std/prelude.cch>

static int g_runs = 0;

int main(void) {
    CCNursery* n = @create(NULL) @detach;
    if (!n) return 1;

    if (n->spawn(() => {
        g_runs += 1;
    }) != 0) {
        n->free();
        return 2;
    }

    if (n->wait() != 0) {
        n->free();
        return 3;
    }

    n->free();
    if (g_runs != 1) return 4;
    return 0;
}
