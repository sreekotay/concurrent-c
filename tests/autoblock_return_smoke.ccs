#include <ccc/cc_runtime.cch>
#include <ccc/cc_closure.cch>
#include <stdint.h>
#include <ccc/std/task_intptr.cch>

static int add1(int x) { return x + 1; }

@async int f(int x) {
    return add1(x);
}

int main(void) {
    intptr_t r = cc_block_on(intptr_t, f(1));
    return (r == 2) ? 0 : 1;
}

