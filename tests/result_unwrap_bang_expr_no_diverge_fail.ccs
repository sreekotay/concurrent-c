#include <stdio.h>
int !> (CCError) f(void) { return cc_err(CC_ERR_NOT_FOUND, "x"); }
int main(void) {
    int x = f() !>(e) { printf("kind=%d\n", (int)e.kind); };
    printf("%d\n", x);
    return 0;
}
