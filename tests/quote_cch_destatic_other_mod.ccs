#!ccc ccs
#include "quote_cch_destatic_mod.cch"
#include <stdio.h>

int main(void) {
    int v = destatic_pub();
    if (v != 7 || destatic_table[1] != 2) {
        printf("hid=%d table=%d\n", v, destatic_table[1]);
        return 1;
    }
    printf("ok\n");
    return 0;
}
