out/
run/
vendor/
*.o
.DS_Store
# Local wrk receipts (compare.sh / bench_latency.sh). Not a git pin.
benchmarks/*.txt
# Generated fixture bodies are large; keep manifest checked in.
fixtures/*.bin
fixtures/*.html
fixtures/*.js
!fixtures/manifest.txt
