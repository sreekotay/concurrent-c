# M0 baseline metrics (capture with: make -C cc && scripts/capture_baseline.sh)
# Date: 2026-05-26
# Reparse count instrumented via CC_DEBUG_REPARSE=1

reparse_sites_visit_codegen=9
initial_parse=1

# Wall-clock compile (ccc build, seconds) — fill after first capture:
# pigz_idiomatic.ccs=
# redis_idiomatic.ccs=
# ufcs_in_lifted_closure_smoke.ccs=
# comptime_channel_ufcs_smoke.ccs=

# Emitted .c size (bytes) — fill after first capture:
