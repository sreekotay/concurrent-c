#define CC_ENABLE_SHORT_NAMES
#include <std/prelude.cch>

int add_ints(int a, int b);

int main(void) {
    int v = add_ints(2, 3);
    @arena {
        String s = string_new(arena);
        s.append("multi ok: ");
        cc_string_append_int(arena, &s, v);
        s.append("\n");
        std_out.write(s);
    }
    return 0;
}


