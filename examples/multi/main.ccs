#include <ccc/std/prelude.cch>

int add_ints(int a, int b);

int main(void) {
    int v = add_ints(2, 3);
    @arena {
        CCString s = cc_string_new(arena);
        s.append("multi ok: ");
        cc_string_push_int(&s, v);
        s.append("\n");
        cc_std_out.write(s);
    }
    return 0;
}


