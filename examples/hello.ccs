#define CC_ENABLE_SHORT_NAMES
#include <std/prelude.cch>

// UFCS-style hello: requires CC compiler UFCS lowering.
int main(void) {
    @arena {
        String s = string_new(arena);
        s.append("Hello, ");
        s.append("Concurrent-C via UFCS! - 4\n");
        std_out.write(s);
    }
    return 0;
}

