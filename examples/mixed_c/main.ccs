#include <ccc/std/prelude.cch>

#include "helper.h"

int main(void) {
    CCArena arena@(kilobytes(4)) @destroy;
    if (!cc_arena_is_live(arena)) return 1;
    {
        CCString s = cc_string_new();
        int v = helper_add(2, 5);
        s.append("mixed_c helper_add(2,5)=", arena);
        s.push_int(v, arena);
        s.append("\n", arena);
        cc_std_out.write(s);
    }
    return 0;
}


