/*
 * Recipe: Closure Capture Semantics
 *
 * Closures in n.spawn(...) capture outer variables. Two modes:
 *
 *   () => body             — value capture (default): each outer variable
 *                            is copied into the closure; the original is
 *                            unaffected; the copy is immutable in the body.
 *
 *   () => [&x] body        — reference capture: the closure sees the
 *                            SAME variable. Read-only by default.
 *
 * Mutation through a reference capture is a compile error unless the
 * captured variable is wrapped in a thread-safe type (CCAtomic::[T],
 * CCMutex::[T]) or the closure is marked @unsafe.
 *
 * A list is not ritual. `() => { use(x); }` already value-captures every
 * outer name the body uses (copy at spawn; the copy is immutable). Write
 * a list when implicit is the wrong mode:
 *
 *   [&x]     — share the frame object (this file)
 *   [x]      — same copy as implicit; write it when the name will be
 *              rebound before the task runs (C for-loop index, accept sock)
 *   [x, &y]  — mix
 *
 * `@parallel for` gives each iteration its own index — no list
 * (recipe_fanout_capture.ccs). Named lists elsewhere:
 * recipe_channel_pipeline.ccs `[&sum, rx]`, recipe_worker_pool.ccs
 * `[jobs_rx, done_tx]`, recipe_tcp_echo.ccs `[sock]`,
 * recipe_exclusive_named.ccs `[id]`.
 */
#include <ccc/cc_runtime.cch>
#include <ccc/script/stdio.cch>

int main(void) {
    @errhandler(CCError e) { cc_error_log(e); return 1; }

    /* --- value capture (default) --- */
    int snapshot = 42;
    println("=== value capture ===") !>;
    println(@string(`before: snapshot = ${snapshot}`, @scratch)) !>;
    {
        CCNursery n = cc_nursery_create() !> @destroy;
        n.spawn(() => {
            @errhandler(CCError e) cc_error_exit(e);
            int seen = snapshot;
            println(@string(`  task A sees snapshot = ${seen}`, @scratch)) !>;
        }) !>;
        n.spawn(() => {
            @errhandler(CCError e) cc_error_exit(e);
            int seen = snapshot;
            println(@string(`  task B sees snapshot = ${seen}`, @scratch)) !>;
        }) !>;
        /* snapshot++ inside the body would be a compile error:
         * value-captured variables are immutable. */
    }
    println(@string(`after:  snapshot = ${snapshot} (unchanged)`, @scratch)) !>;
    println("") !>;

    /* --- reference capture (read-only) --- */
    int counter = 100;
    println("=== reference capture (read-only) ===") !>;
    println(@string(`before: counter = ${counter}`, @scratch)) !>;
    {
        CCNursery n = cc_nursery_create() !> @destroy;
        n.spawn(() => [&counter] {
            @errhandler(CCError e) cc_error_exit(e);
            println(@string(`  task C reads counter = ${counter}`, @scratch)) !>;
        }) !>;
        /* counter++ in the body would be a compile error:
         * mutation of a shared reference requires a safe wrapper. */
    }
    println(@string(`after:  counter = ${counter}`, @scratch)) !>;
    println("") !>;

    /* --- when you need mutation ---
     *   CCAtomic::[int] counter        — lock-free atomic ops
     *   CCMutex::[int]  counter        — lock-based guard
     *   () => @unsafe [&counter] ...   — opt-out; the race is on you
     *   channels                       — pass values, don't share state
     *
     * See recipe_fanout_capture.ccs and recipe_channel_pipeline.ccs.
     */
    return 0;
}
