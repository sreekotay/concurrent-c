#define CC_ENABLE_SHORT_NAMES
#include <ccc/std/prelude.cch>

int add_ints(int a, int b);

int main(void) {
    int v = add_ints(40, 2);
    @arena {
        String s = string_new(arena);
        s.append("build_graph multi ok: ");
        cc_string_push_int(&s, v);
        s.append("\n");
        std_out.write(s);
    }
    return 0;
}


