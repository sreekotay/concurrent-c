diff --git a/Makefile b/Makefile
index d21f1cad..cfff431b 100644
--- a/Makefile
+++ b/Makefile
@@ -21,6 +21,9 @@ LIBTCC1 = libtcc1.a
 LINK_LIBTCC =
 LIBS =
 CFLAGS += $(CPPFLAGS)
+ifeq ($(CONFIG_cc_ext),yes)
+  CFLAGS += -DCONFIG_CC_EXT
+endif
 VPATH = $(TOPSRC)
 -LTCC = $(TOP)/$(LIBTCC)
 
diff --git a/tcc.h b/tcc.h
index e7a2f1e2..77881772 100644
--- a/tcc.h
+++ b/tcc.h
@@ -452,6 +452,10 @@ typedef struct TokenSym {
     char str[1];
 } TokenSym;
 
+#ifdef CONFIG_CC_EXT
+#define CC_TCC_EXT_AVAILABLE 1
+#endif
+
 #ifdef TCC_TARGET_PE
 typedef unsigned short nwchar_t;
 #else
@@ -1872,7 +1876,7 @@ dwarf_read_uleb128(unsigned char **ln, unsigned char *end)
 {
     unsigned char *cp = *ln;
     uint64_t retval = 0;
-    int i;
+    unsigned i;
     for (i = 0; i < DWARF_MAX_128; i++) {
 	uint64_t byte = dwarf_read_1(cp, end);
         retval |= (byte & 0x7f) << (i * 7);
@@ -1887,7 +1891,7 @@ dwarf_read_sleb128(unsigned char **ln, unsigned char *end)
 {
     unsigned char *cp = *ln;
     int64_t retval = 0;
-    int i;
+    unsigned i;
     for (i = 0; i < DWARF_MAX_128; i++) {
 	uint64_t byte = dwarf_read_1(cp, end);
         retval |= (byte & 0x7f) << (i * 7);
diff --git a/tccpp.c b/tccpp.c
index e19e8504..a51233ec 100644
--- a/tccpp.c
+++ b/tccpp.c
@@ -3846,7 +3846,10 @@ static void pp_line(TCCState *s1, BufferedFile *f, int level)
 
     if (s1->Pflag == LINE_MACRO_OUTPUT_FORMAT_NONE) {
         ;
-    } else if (level == 0 && f->line_ref && d < 8) {
+    } else if (level == 0 && f->line_ref && d >= 0 && d < 8) {
+	/* CC patch: upstream `d < 8` also matched negative deltas, which
+	 * silently dropped user #line resumes after large synthetic
+	 * injections (Concurrent-C diagnostics). Restrict to d >= 0. */
 	while (d > 0)
 	    fputs("\n", s1->ppfp), --d;
     } else if (s1->Pflag == LINE_MACRO_OUTPUT_FORMAT_STD) {
