# Third-Party Dependencies

- `tcc/`: upstream TinyCC source (add as submodule or vendor drop). Keep pristine; apply only documented minimal hooks.
- `tcc-patches/`: small, optional patch set for CC integration (lexer/parser hooks, const-eval API). Should stay minimal and upstreamable.

