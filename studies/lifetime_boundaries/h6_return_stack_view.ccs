#include <ccc/std/prelude.cch>
static void use(char[:] s) { (void)s; }

static char[:] f(void) {
    cc_arena_stack(t, 64);
    char[:] s = cc_arena_alloc_slice_bytes(t, 8);
    return s;
}
int main(void) {
    char[:] s = f();
    use(s);
    return 0;
}
