[package]
name = "quickjs_lifetime_track_a"
version = "0.1.0"
edition = "2021"
publish = false
description = "Track A hostile retain/release/weak + callback registry (rquickjs)"

[dependencies]
rquickjs = { version = "0.12.2", default-features = true, features = ["array-buffer"] }

[profile.release]
lto = false
codegen-units = 16
