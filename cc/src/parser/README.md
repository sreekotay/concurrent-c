# CC Parser Overlay

Extends TCC grammar for CC constructs (`@async`, `@nursery`, `@match`, `T?`, `T!>(E)`, `T[:]`, `T[~]`, `@comptime if`, comptime type-returning functions). Uses parser hook entry points; avoid core grammar forks.

