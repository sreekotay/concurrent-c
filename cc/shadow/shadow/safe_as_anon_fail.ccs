/* Safety fail: field `@as` is removed. */
#include <ccc/std/prelude.cch>
#include <stdio.h>

typedef struct {
    CCFile file @as;
    int x;
} AnonOuter;

int main(void) {
    AnonOuter o = {0};
    (void)o;
    return 0;
}
